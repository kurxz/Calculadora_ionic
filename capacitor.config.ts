import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'kurxz.github.io',
  appName: 'Calculadora',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
